# Optional developer-only configuration: copy to .env.deploy and chmod 600.
# Parsed as literal KEY=value, never sourced as shell code. No SSH credentials.
# Production/data/web paths are fixed and guarded in scripts/local_deploy.py.
MONEY_NOTE_DEPLOY_BRANCH=main
MONEY_NOTE_API_BASE_URL=https://money.hjkerman.re.kr

# Android signing stays in mobile/android/key.properties (ignored, chmod 600).
# Do not copy production .env or data into this checkout.
