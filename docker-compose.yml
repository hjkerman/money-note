services:
  api:
    build:
      context: ./backend
    ports:
      - "18080:8000"
    environment:
      MONEY_NOTE_DB_PATH: /app/data/money-note.sqlite3
    volumes:
      - ./data:/app/data
    restart: unless-stopped
