name: money_note_mobile
description: Money Note 모바일 앱
publish_to: "none"

version: 1.6.0+6

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^10.3.1
  intl: ^0.20.3
  path_provider: ^2.1.6
  share_plus: ^13.2.1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
